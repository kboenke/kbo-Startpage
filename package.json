{
  "__comment": "Node.js dependencies required for linting and unit-testing in a workflow",
  "name": "kbo-startpage",
  "version": "1.0.0",
  "description": "Startpage Chrome extension",
  "main": "src/startpage.js",
  "scripts": {
    "lint:js": "eslint src/*.js",
    "lint:css": "stylelint 'src/**/*.css'",
    "test": "jest --config .jest.config.js",
    "test:watch": "jest --config .jest.config.js --watch",
    "test:coverage": "jest --config .jest.config.js --coverage"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-plugin-jquery": "^1.5.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-junit": "^16.0.0",
    "jquery": "^3.7.1",
    "stylelint": "^16.12.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
